interface Props {
    properties: any
}

export default function Activity(params: Props): JSX.Element {
    console.log('subtraction facts of num loaded');
    return <p>subtraction facts of num</p>
}